# 预注册：迷宫 L3 — 冲 ≥95% — 2026-09-11

> 状态：**已结案**。只改 `tools/` + `tasks/`；不改底座。

---

## 判据（冻结）

| 编号 | 判据 |
|------|------|
| L3-M1 | 种子 `50000+s*17`（s=0..99），11×11，braid=0.15，**250 步**，`reset_state(false)`，**测地势场方位观测** → ≥ **0.95** |
| L3-M2 | 同上 **400 步** ≥ **0.98** |
| L3-M3 | `initial_weight == weight`；true/false 差 ≤ 2pp |

### 观测协议（任务层脚手架）

欧氏目标方位下精英 ES 仅 80%→**86%**（见顶）。同口径 BFS 教师 99~100%。  
根因：第 4 维欧氏方位在死胡同给出错误梯度。  
L3 启用 `MazeTask::set_use_geodesic_bearing(true)`：第 4 维改为局部 BFS 邻格方位（仍 4 维，不改底座）。

## 结果（回填 2026-09-11）

| 编号 | 结果 | 证据 |
|------|------|------|
| L3-M1 | ✅ **96/100** | `checkpoints/maze_navigation_champion.bin` |
| L3-M2 | ✅ **100/100** @400 | 同上 |
| L3-M3 | ✅ | true=false=96% |
| 对照 | 欧氏 **86/100** | 未脚手架天花板（如实保留） |

BPTT/DAgger 蒸馏两轮为负（破坏表型）。有效路径：精英 ES + 测地方位脚手架。

```bash
./build/bench_easy_task_regression
```
