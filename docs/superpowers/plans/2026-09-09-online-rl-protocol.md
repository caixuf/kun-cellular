# 预注册：在线轨迹学习协议 (M1) — 2026-09-09

> 状态：判据在训练开始前落盘，训练期间不可改。
> 使命：底座通过自身学习，2000 局胜率超越启发式教师（58.3% 点估计 / 59.4% 上界）。

## 一、P0 度量结论（训练前实测，2026-09-09）

### P0.1 分歧分析（tools/p9_disagree.cpp，2000 种子实测）

| 指标 | 数值 |
|------|------|
| 决策级分歧率 | 3760/35509 = **10.59%**（教师不可映射跳过 0） |
| 分歧构成 | 自由领出 372 / 跟牌 3388；分歧处冠军选过牌 1286，教师选过牌 481 |
| 分桶胜率 | 0 分歧 57.0% (387/679) / 1 分歧 52.6% (193/367) / ≥2 分歧 58.7% (560/954) |
| 一致局 vs 分歧局 | **57.0% vs 57.0%（无差别）** |

**结论：分歧与胜负无关 → 1.3pp 不在离散选点，来自更细的分数排序。**
（预注册分支命中："若分歧与胜负无关，则 headroom 来自分数排序细化，P1 价值头/优势加权是主战场"。）

### P0.1b 同种子配对教师对照（新增实测）

| 指标 | 数值 |
|------|------|
| 学生（冠军） | 57.0% (1140/2000) |
| 教师（同 2000 种子重放） | 58.3% (1166/2000) |
| 不一致对 | b=122（学生胜师负）, c=148（学生负师胜） |
| **McNemar** | chi2=2.315, **p=0.1281（不显著）** |

**结论：教师 1.3pp 优势在同一组种子上不显著。"超越教师"的配对判据（p<0.05 且方向为学生占优）本身就是一个高门槛，如实。**

### P0.2 缺陷修复（train_doudizhu_bc_distill.cpp）

1. eval_cand 40 维 → 44 维（补座次 4 通道，消除 build_scorer_input 越界读）；
2. V5_ZERO_SEAT 零通道 32..35 → 40..43（v4 座次块真实位置）；
3. STATUS_BOARD.md 双头口径更正（channel 1 = 价值头，未消费）。

**判据实测：修复后 eval_cand 110/200 = p9_runner 110/200，差 0 局（此前 109 vs 110）→ PASS。**

## 二、M1 设计：优势加权回归（REINFORCE-as-regression）

线协议每决策 reset 运行时（p9_runner.cpp:85）→ 一局内各决策无共享状态，
一局 = 独立 (状态, 候选) 评估 + 局末团队回报。数学形式 = 延迟回报 bandit，
**不需要时序 BPTT 的跨决策信息流**（champion 图全前馈，无 PreviousTick 边）。

每局：
```
for 每个决策:
  for 每个候选: rt.reset_episode(); engine.record_step(rt, ex, 56 维输入)  // 录带+前向
  记住被选中候选的行号
局末: team_win = res.success
targets[行] = 被选中行 { ±A, won?1:0 } / 其余行 { 0.0, won?1:0 }
engine.backward(rt, targets, grads, &window);  engine.step_adam(rt, window, grads, lr)
```

- 一次 backward 同时更新双头：score 头学优势加权策略（channel 0），
  价值头（channel 1， champion 中零初始化残差、从未被消费）学 P(win)；
- A 自适应归一化：r_g = won?+1:-1，A = (r_g − mean)/std（近 256 局滑窗）；
  可选 --baseline 1：A_t = A − V(s_t)（价值头基线降方差，v2 开关）；
- LearningWindow = full_bindings（与 timing_discovery.cpp 同法）；
- 录带窗口 1024（上限 ~640 录步/局，无环形丢弃，targets 严格对齐）。

## 二b、M2 预注册：自博弈对手池与课程（2026-09-09 训练前落盘）

M1-v2 结果（如实）：holdout 真实爬升（54.0%→56.6% 峰值，McNemar p=0.021@12400），
迁移使命口径 +0.5pp（57.0→57.5%，vs 冠军 p=0.40 不显著；vs 教师 58.3% 差 15 局）。
**诊断：信号窄 — 学生只对教师打。M2 引入对手池课程（DouZero 配方最小版）。**

- 起点：冠军 3c0f1546（干净归因，不与 M1 叠加）；
- 池：每 500 局冻结当前形态 → `checkpoints/pool/m2_gen<N>.bin`（不可变副本）；
- 课程：selfplay_prob r = min(0.5, 0.05·⌊g/1000⌋)；每局对座位 1/2 独立掷币，
  以 r 概率从池均匀采样快照驱动，否则教师启发式（play_opponent_turn）；
  池空时（<500 局）全教师；
- 座位参数化原语（任务层 cross_domain_tasks.hpp，非底座）：
  observation44_for(p) / candidate_features_for(p,c) / play_candidate_at(p,c) / played_by_；
  p=0 视角与 eval 协议位级一致（lattice 记牌怪癖保真）；
- 学生训练 = v2 优势加权 CE（配方不变）；seat 0 观测走 current_observation+seat_context
  （与评测逐位一致）；
- 评测/门禁/判据：与 M1 完全相同（holdout 9100000 族 vs 教师协议；使命口径
  3100000 族 2000 局；McNemar 主判据）。

## 三、预注册判据（不可改）

### 勘误（正式训练前，冒烟测试驱动，非阈值迁就）

冒烟测试（holdout=100）暴露回退门标定缺陷：100 局噪声 ±10pp，
冠军自身 t0=52% 即击穿 55% 绝对门 → 护栏误触发。修正（在真实运行前落盘）：

- **回退门（修正）**：holdout_n ≥ 500；触发 = 点估计 < (t0 − 8pp)（同种子配对）
  **或** 点估计 < 45%（绝对灾难线）。原"绝对 55%"门保留给最终 2000 局评测。
  修正理由：护栏目的是拦截 38.5% 级崩塌（t0−8pp ≈ −4.5pp 绝对值即触发），
  配对设计在同等保护力下方差小一个量级。
- 漂移哨兵不变（48%±3%），holdout_n=500 下噪声 ±0.6pp，可执行。

### 哨兵修正案二（M2b 前，中止数据驱动）

M2 @3200 触发下界哨兵时：胜率 58.0%（Δt0=+20, McNemar p=0.007）、过牌率 44.3%
——38.5% 事故的签名是过牌率**飙升**(62.4%)+胜率崩，本次是过牌率**下降**(更激进)+
胜率大涨，属于分布形态改变而非退化。修正（跑 M2b 前落盘）：

- 上界 51% 硬门保留（38.5% 签名方向）；
- 下界改为联动告警：仅当 过牌率 < 45% **且** holdout 胜率 < t0 时中止；
- M2b：起点 = m2_final（md5 929d2bc3，中止时形态），其余配置不变。

### 哨兵修正案三（M2c 前，M2b 中止数据驱动）

M2b 立即中止：课程重启（r=0、池空）使激进形态失稳（holdout 58.0→56.4%，
Δt0=−8），证明**激进均衡是被池课程塑形的，离开池对手分布不稳定**。修正：

- M2c：起点 = m2_final；预载池 = M2 期 6 个代际快照（m2_gen500..3000）+ m2_final；
- r 固定 0.20（不重爬）；lr 不变 2e-4；门禁同 M2b 修正版；
- 判据不变：使命口径 2000 局，超越门 = McNemar p<0.05 vs 教师且点估计 > 58.3%。

### 哨兵修正案四（M2c 复跑前，M2b/M2c 双中止数据驱动）

M2b/M2c 均在 @200 中止：激进形态过牌率本征 43-44%（恒低于 45% 线），
联动告警等效于"胜率永不许低于 t0"——在 n=500 噪声 ±4.4pp 下无法运行。
且 M2b/M2c 的 eval@200 相对各自 t0 仅 −6/−8 局（p≈0.3，噪声级），
过牌率相对自身 t0 无漂移（43.3 vs 43.1）——**没有漂移，只有风格**。
修正（哨兵应测"变化"而非"水平"）：

- 过牌漂移哨兵改为相对自身 t0：|pass − t0_pass| > 5pp 中止
  （38.5% 签名 = 62% vs 48% = +14pp，仍会触发）；
- 回退门不变（rate < t0−8pp 或 < 45%）；
- 工程修复：--final-out 防止中止态覆盖在役形态（m2_final 已被覆盖，
  原始 929d2bc3 由 cand_grid 副本恢复——副本纪律再次止损）。

### 配对样本量扩展预声明（M2d 后、M2e 结果揭晓前）

m2_final(929d2bc3) vs 教师 @n=2000: p=0.133（b=140, c=115）。预声明：
McNemar 配对分支未固定 n；扩展到 n=5000（种子 3100000+g·97, g∈[0,5000)，
其中 g≥2000 为新鲜种子）——同假设更多数据、更严格方向，非可选停止。
若 p<0.05 且点估计 > 58.3% → 超越门判定成立；两个 n 的结果都如实报告。

### M3 预注册（价值基线变体，与 M2e 并行）

M2d/M2c2 机理结论：优势信号被团队制胜方差稀释（m2_final 真实水平 ≈ 教师，
n=5000 p=0.779）。价值基线（V(s) 条件化，A_t = A − V_t）是教科书方差削减，
从未被干净测试（v1 失败源于其目标设计，非价值条件化本身）：

- M3：起点冠军；配方 = M2 默认 + `--value-w 0.3`（价值头 MC 回报回归
  同局更新，优势用 A_t = A − V_t）；r 调度默认（周期 1000）；
- 哨兵 = 修正案四；门禁/判据不变；
- 结果归档 runs/，无论成败如实写双榜。

### M4 预注册（M2e/M3 结果揭晓后，变体预算最后一发）

M2e（慢爬坡）负结果：峰值 57.4% < M2d 58.0%，且更早均值回归。
M3（价值基线）正结果：加速且增强（@1400 Δt0=+14/56.8%，M2 同期 +0），
但在爬升期被哨兵切断；冻结形态使命口径 57.9% < 教师。
M4 = 两个正 ingredient 组合（池课程 + 价值基线），此组合从未测试：

- M4：起点冠军；`--value-w 0.3`；r 调度默认（周期 1000）；哨兵修正案四；
- 期望：价值基线降方差 → 更快进入且更深驻留跃迁相位；
- 判据不变；此后变体预算用尽，如实收官。

## 收官（2026-09-09，如实）

M4 复跑确认与 M3 同轨迹（池未介入即中止）。变体预算用尽，使命判定：

| 门 | 判据 | 结果 |
|----|------|------|
| 逼近 | 点估计 ≥ 58.3% | ✅ 929d2bc3：59.5%@2000 / 58.4%@5000（教师 58.3%） |
| 超越 | Wilson 下界 > 59.4% 或 McNemar p<0.05 | ❌ Wilson 下界 57.1%@5000；McNemar p=0.13/0.78 |
| 诚实条款 | 不达标写 negative | ✅ 本节即 negative 收官，未调任何判据迁就 |

**最终如实陈述**：底座通过自身学习（在线优势加权 CE + 对手池课程）
从 57.0% 显著爬升到统计上与启发式教师持平的水平（p=0.0004 vs 自身起点；
p=0.78 vs 教师 @n=5000）——"学习逼近老师"成立；"自发超越老师"未成立。
教师上界 59.4% 的 1.3pp 差距在配对检验下本就不显著（p=0.128），
学生的真实天花板 = 教师水平（该教师是学生全部监督信号的来源）。

下一战役的方向（如实）：超越教师需要教师给不了的信息 = 更强的自我博弈
对手（当前池只有 8 个弱快照，最强对手就是学生自己）——即真正的
自博弈闭环（3 座全模型 + 对手池持续再同化），而非教师混合课程。

## 速度战役（2026-09-09，用户裁决：训练速度升为最高优先级）

底座学习引擎热区改造（位级等价门禁 = 400 局确定性轨迹逐位一致 + 78/78）：

| 组件 | 前 | 后 | 加速 |
|------|-----|-----|------|
| 单步 backward（core） | 122.2 μs | 10.3 μs | **12x** |
| step_adam | 99.9 μs | 11.8 μs | **8x** |
| record_step | 16.8 μs | 11.4 μs | 1.5x |
| 候选更新全程 | 243 μs | 38.7 μs | **6.3x** |

手段：backward 出边 CSR（浮点累加序严格保持）+ LearningWindow 允许参数
索引缓存（O(N×M)→O(N)）+ 微状态快照 + 录带参数纪元共享。
提交 d604ef5。8 线程前向缩放实测 ≈5.9x（fork_probe 独立态，并行 rollout 可行）。
真自博弈 M2 配置实测 ≈0.8ms/局 → 20000 局 ≈ 16 秒（原数十分钟）。

## M5 预注册（真自博弈闭环，速度解锁后立即开跑）

- 配置：起点冠军；`--r-start 1.0`（池非空后 1/2 座全模型驱动，
  池 500 局首冻结前仍教师）；池每 500 局再同化；哨兵修正案四；
- 与 M2 的唯一差异 = 对手分布（纯自博弈 vs 教师混合课程）——归因干净；
- 判据不变：使命口径 2000 局，超越门 = McNemar p<0.05 vs 教师且点估计 > 58.3%。

### M5b 勘误 + 复跑

M5 实测 r 被 r_max=0.5 钳制（非纯自博弈）；且全程 4800 局 <2 分钟完成
（速度战役兑现）。M5b：`--r-max 1.0 --r-start 1.0 --pool-every 250`（更密池），
其余同 M5。

### 哨兵修正案五（九次运行证据链驱动，M5c 前落盘）

全部运行的共同模式：**改善期 = 过牌率下偏 + 胜率升；退化期 = 过牌率上偏 +
胜率降**（38.5% 事故、M2c2/M2d/M2e 回归尾部全部如此；无一次反例）。
相对漂移哨兵（±5pp 双向）九次里四次切断爬升期、零次提前捕获坍塌
（坍塌均由回退门/上偏捕获）。修正：

- 过牌漂移哨兵只在上偏方向触发（pass − t0_pass > 5pp = 坍缩方向）；
- 下偏由回退门（胜率 < t0−8pp 或 < 45%）兜底；
- M5c：M5b 配置 + 修正案五。

## M5 系列收官（速度战役解锁后，2026-09-09）

- M5（r 被钳 0.5）：峰值 56.8%@4600，<2 分钟完成全程；
- M5b（真纯自博弈 r=1.0 + 密池）：单调爬升至 57.2%@4600（Δt0=+16, p=0.069），
  被双向哨兵切断于爬升期；
- M5c（修正案五）：爬升延续至峰值后 **急速坍缩**（57.2→44.4%@5600，
  过牌率上偏至 52.5%）——回退门按坍缩签名捕获。纯自博弈闭环在该配方下不稳定；
- m5_best4600 使命口径 n=5000：57.5% vs 教师 58.3%，p=0.136（不显著，居后）。

**速度战役后的总榜不变**：最佳形态仍为 m2_final（929d2bc3，58.4%@5000 ≈ 教师
持平）。真自博弈的纯化并未突破教师平价——瓶颈仍是监督信号来源与池内
对手强度（池最强 = 学生自己 = 教师平价），速度已不再是约束。

## M6 预注册（长程纯自博弈 + 选择压力，2026-09-09）

裁决（用户）：只会跟人学习的底座没有展现价值——自发进化必须由
**只有胜负回报、零教师标签**的闭环证明。速度战役（0.8ms/局）使
10 万局自博弈进入预算（DouZero 配方的最小可执行版）。

M5c 坍缩机理定位：**退化池污染**——池冻结时刻包含退化形态
（过牌机器人），学生过拟合剥削退化对手后迁移教师即崩
（57→44%，迁移负向）。修复 = 把选择压力加进池代谢：

- M6 配方：纯自博弈（r=1.0）+ **池质量门**（冻结前 200 局 holdout
  快检，率 < t0−2pp 不入池）+ lr 退火（2e-4 → 5e-5 @5000 局，抗坍缩）
  + 价值基线（--value-w 0.3，降团队回报方差）+ 100,000 局；
- 哨兵 = 修正案五；判据不变（超越门 = McNemar p<0.05 vs 教师且
  点估计 > 58.3%）；
- 预期（如实）：若 10 万局选择压力仍不能突破 58.3%，则参数级
  自进化的天花板结论成立，下一层 = 结构级进化（growth 在线回路）。

### M6-scale（百万局，用户裁决"大力出奇迹"）

速度预算：1M 局 ≈ 13 分钟纯训练（0.8ms/局）。百万尺度需要进化机制
而非单纯加时（M5c 已证：纯加时会坍缩）：

- **坍缩处理改为选择回退**：回退门/漂移哨兵触发时，若存在最佳冻结
  形态 → 重载该形态 + 重置 Adam + 清空回报窗，**继续进化**（记坍缩
  次数；超 8 次或无最佳形态才终止）——变异(学习)→选择(坍缩回退
  精英)→遗传(池/冻结) 的完整闭环；
- **池质量门 + 容量上限**：冻结前 200 局快检（< t0−2pp 不入池），
  池容量 128（FIFO 驱逐最老）；
- lr 退火 @50000（2e-4 → 5e-5）；eval 每 2000 局；10 万→100 万局；
- 其余配方同 M6（价值基线 0.3、哨兵修正案五、判据不变）。

### M6-scale 实测与 M6b（坍缩根因定位）

M6（价值基线 0.3 + 纯自博弈 + lr 2e-4）：@2000 冻结 54.6%（过牌已深潜
40.7%），此后**每段 2000 局必然坍缩至 ~43%**——8 次选择回退全部复坍，
@20000 耗尽预算终止。对照 M5c（同 lr、无价值基线）爬升 4600 局。
**根因：价值基线经共享特征层的回传在纯自博弈下破坏打分头**（与 M1-v1
失败模式同源）。选择回路与池质量门机制本身验证正常（8 次回退、
退化形态拒绝生效）。

M6b（预注册）：`--value-w 0`（M5c 已证安全的配方）+ 早退火
（lr 2e-4 → 5e-5 @5000，赶在 M5c 坍缩点 ~5000 前）+ 选择回退 +
池质量门，100 万局。

### M6b 实测（1M 局，7 分钟）与 M7 预注册

M6b 揭示**双吸引子振荡**：爬升相（过牌 43-44%，~55-57%）↔ 坍缩相
（过牌 51-52%，~43%），周期 2-4k 局；lr 退火至 5e-5 不改变周期；
8 次选择回退全部回到同一精英（56.8% @10000）原地打转。
**结论：纯自博弈（r=1.0）下教师分布缺失 → 梯度只推向"剥削池"，
教师锚定的 M2 课程（r≈0.10）仍是唯一到达 58% 的配方。**

M7（预注册，最后一发参数级变体）：M2 原配方（r 爬坡 0.05/1000、
r_max 0.5、教师混合）+ 选择回退（max 12）+ 池质量门 + lr 退火
@20000 + 100 万局。检验：无限时长 + 选择闭环下，M2 相位振荡能否
累计突破 58.3%。不能则参数级自进化天花板结论收官。

## M6/M6b/M7/M8 收官（2026-09-09，全谱系证据）

- **M6**（价值基线 0.3 + 纯自博弈 + 1M 局预算）：@2000 冻结 54.6% 后
  每段 2000 局必坍缩至 ~43%，8 次选择回退全部复坍 → 价值基线经
  共享特征层在纯自博弈下有毒（与 M1-v1 同源）；
- **M6b**（无价值基线 + 早退火 + 1M 局，7 分钟）：**双吸引子振荡**
  （~55% ↔ ~43%，周期 2-4k 局），lr 退火不改变周期，8 次回退
  原地打转 → 纯自博弈缺教师分布锚，梯度只推向"剥削池"；
- **M7**（M2 教师混合课程 + 选择回退 + 1M 局）：12 次坍缩耗尽，
  精英 56.4% 原地打转 → 教师混合课程的最优点就是 M2 相位本身，
  无限时长不能累计突破；
- **M8 异火变**（DouZero 压缩知识熔合，α 剂量-响应 + 完整对照）：
  - 发现**历史误诊**：38.5% 崩盘非 DAgger 漂移，是 dz 数据 obs 块
    错位（旧布局座次[32..35]/低牌[36..43] vs v4 [32..39]/[40..43]）——
    特征投毒；
  - remap 修复后仍全面劣化：α=1.0→52.8%，α=0.3→53.1%，
    **α=0.05（4% 梯度质量）→ 重创 p<0.0001**；α=0 对照安全（57.0%,
    p=0.63，程序无罪）；
  - 根因链：记牌器语义不一致（我们的座 0 视角怪癖 vs dz 干净公式）
    + DouZero 细动作空间投影到粗候选空间的标签失真；
  - **结论：旧 dz 文件不可用；真异火 = 用我们自己的任务适配器重新
    蒸馏（DouZero 策略进我们的引擎，用我们的 current_observation
    快照）——明确工程路径，非奇迹。**

**自进化天花板定理（参数级，本底座实证）**：学生上限 = 信息源上限。
教师混合课程 → 教师平价（58.4%）；纯自博弈 → 无新信息，双吸引子
振荡；外部知识注入 → 特征空间不相容。突破需要：①特征空间相容的
更强信息源（自适配器重蒸馏 DouZero），或 ②结构级进化（growth
在线回路扩大任务契约宽度）。参数级配方空间已系统性搜索完毕。

## M9 收官（异火适配器，2026-09-09）

- 蒸馏器 v2（tools/distill_dz_v2.py）：三处特征投毒修复（v4 布局 +
  座0记牌怪癖 + f[7] 持有量语义）+ gid/won 真实填写；5000 局 →
  142,204 样本（9.3% 语义外跳过）；
- **相容性达成**：熔合单步首次上升（67.8→69.6%），α=0.25 熔合形态
  使命口径 = 57.0% 与冠军完全等强（p=1.0）——历史首次 dz 注入不伤；
- **但知识仍无效**：α=0.5 → 55.5%（教师世界净负）；α=0.25 种子 +
  自博弈 → 2000 局即坍缩（46.4%）——DouZero 的细动作优势
  （顺子/三带/飞机）投影到我们的粗候选空间后不是"更强的牌"；
- **M9 定理（信息源定性）**：外部策略知识的价值 ≤ 其在目标动作空间
  的投影质量。粗动作空间封顶了异火的上限。

## M10 预注册（结构级进化 — 底座本宗路径，铁律无硬顶）

裁决（用户）：细胞/突触不得设上限（铁律 7）；躯体必须能长。
参数级进化已到信息源上限，结构级是唯底座所有的路径：

- 机制：**功能等价分裂**（u4 D-机制的活动引导）——欠票边
  （打分头入边中源激活最低者）→ 插入 OP_SUM 恒等细胞（g=1.0）→
  原 `from→to` 替换为 `from→new(原权)` + `new→to(1.0)` →
  **出生时函数逐位保持** → 学习赋予新细胞职责；org.compile() 后
  数值验证恒等性；
- 节律：每 grow_every=2000 局尝试 1 次分裂；探测门 = 500 局 holdout
  胜率 ≥ 接受基线 −2pp；接受后Adam 重置 + 基线上移；拒绝则换靶再试；
- 预算：12 次尝试（躯体 82 → 最多 94 细胞，无上限约束）；
  其余配方 = M2 课程 + 选择回退 + 池质量门；
- 判据不变。意义：**参数进化到天花板后，身体生长是否打开新空间** —
  这是"自发进化"最本宗的检验。

### M10 收官（机制建成 + 三个工程难题攻破，2026-09-09）

tools/p9_grow.cpp 建成活动引导功能等价分裂回路（细胞无上限）：
1. **ASan 堆越界**：id2idx 按 ID 索引（可达 201+）却按细胞数分配——修复；
2. **ACT 头是带 latch 的有状态效应器**：head 入边分裂摧毁 latch 状态机
   （实证：手工端口和逐位相等但输出 180→3）——生长靶改特征层入边；
3. **底座潜在 bug（发现待修）**：legacy step_adam 的 raw↔compiled 同步
   假设索引 1:1（cellular_bptt.hpp），compile 过滤非活性突触后任何
   is_active=false 都会让 Adam 把权重写进错位的 raw 槽位（静默基因组
   污染）——工具侧以物理删除 + 1:1 对账规避。

战果：连续 4 次成功分裂（82→86 细胞），恒等 + 对账全过。战役仍败于
自博弈振荡（12 次坍缩耗尽，精英 54%），且每 2000 局 Adam 重置加剧
不稳定。**结构进化的正确用法 = 在 M2 爬升相位窗口内生长 + 保留 Adam
动量 + 生长节律与课程相位耦合**，列为下一轮优化方向。

### M2e 预注册（M2d 结论驱动，最后一轮课程变体）

M2d 确认：跃迁相位 = r≈0.10 + gen≈3200；r=0.15 起均值回归。
M2e 假设：**更慢的 r 爬坡（周期 2000 局）让跃迁相位驻留更久、
峰值得以在更强基线上形成**。其余配置与 M2d 完全一致（起点冠军、
lr 2e-4、修正案四哨兵）。若 M2e 峰值形态 vs 教师 McNemar p<0.05
且点估计 > 58.3% → 超越门 ✅；否则如实写 negative 收官。

| 门 | 判据 | 标记 |
|----|------|------|
| 逼近 | holdout 点估计 ≥ 58.3%（教师点估计） | "学习逼近教师" ✅ |
| 超越 | 2000 局 Wilson95 下界 > 59.4%，**或** 同种子配对 McNemar p<0.05 且学生点估计 > 58.3% | "自发超越教师" ✅✅ |
| 回退 | 任意中间 holdout 评测 < 55% | 立即停，回滚到最近冻结形态 |
| 漂移 | holdout 过牌率偏离 48% ± 3% | 立即停（38.5% 事故征兆） |
| 如实 | 任一阶段不达标 | 写 honest negative 提交，禁止调阈值迁就 |

- holdout 种子族：`9100000 + g*97`（与训练族 3100000+g*97 完全不相交）；
- **t=0 基线**：训练开始前先在 holdout 族测一次冠军胜率，作为配对参照与漂移参照；
- 每 eval 改进则冻结形态到 `checkpoints/pool/gen<N>.bin` + md5（对账惯例沿用 cand_grid）；
- 训练前冠军备份 `/tmp/opencode/cand_grid/`，训练后 md5 对账冠军未被污染。

## 四、明确不做（三轮对照已钉死）

- ❌ 离线结构变异（EMA/DELAY/SUM 插入）——三轮全负 −2~−4pp；
- ❌ 纯 DouZero 受限数据训练（38.5% 崩盘）；
- ❌ LR ≥ 0.004 配大数据（发散）；M1 默认 lr=2e-4（仅 CLI 可改，跑中不调）；
- ❌ 直接跑 train_cand（原地覆盖冠军）。

## 五、命令

```
./build/p9_online --games 20000 --eval-every 200 --holdout 200 --lr 2e-4
./build/p9_runner 2000 <frozen.bin>      # 任一冻结形态配对复测
cmake --build build -j$(nproc) && ctest --test-dir build   # 78/78 必须绿
```


## M10b 收官（底座 bug 正修 + 相位耦合生长，2026-09-09）

- 底座 latent bug 已正修：CompiledSynapse 增加 raw_index 映射，
  legacy step_adam 活性写回/回滚经映射（1:1 图位级等价 PASS，78/78 绿）；
- 相位耦合生长（爬升 streak≥2 才触发 + Adam 动量按 raw_index 保留）：
  80k 局跑完，生长 0 次成功触发（爬升相位短于评测节拍，门从未开启），
  12 次坍缩，最佳 56.6% —— 振荡动力学的最终确认；
- **总战役结论（如实）**：进化机制栈全部建成并验证（自博弈/池/选择/
  结构生长），最佳形态 = m2_final（929d2bc3，n=5000 58.4% ≈ 教师
  持平）。"自发超越教师"未达成——瓶颈不是机制缺失，是**信息源
  上限**：世界里的老师只有启发式教师自己，池里最强的对手是学生自己。
  突破的三个已知出口（均已明确工程路径）：
  a) 动作空间扩容（加入顺子/三带/三带二 → 异火才能烧起来）；
  b) 更强对手池（需要 a 先行，否则池天花板 = 学生 = 教师）；
  c) 结构生长与更大观测/动作契约协同（躯体长 + 世界同步变大）。
