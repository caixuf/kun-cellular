# 迷宫 Gate 6（影子模式差分对账）实现卡 (2026-09-15)

> 落地：`tests/test_flow_maze_gate6_shadow.cpp`。任务层 only；不改 `include/kun/cellular/`。不要求冠军与专家轨迹重合。

## 协议

对照 ADAS Gate 6 的「影子平滑度」精神，迷宫影子基线是 **BFS 测地贪婪专家**（与 `MazeTask::rebuild_geodesic_field_` / `train_maze_tripartite` DistField 同构），不是 Stanley，也不是 obs[3] 比例控制。

- 锁档：`checkpoints/maze_navigation_champion.bin`
- 未见种子 20 个：`50000 + i*17`，11×11，编织率 0.15，测地势场开，250 步
- 专家：朝距离场下降邻格转向；大航向误差时停车转向
- 冠军：`compile()` + `reset_state(false)` + `forward(..., false)`

## 预注册判据（先量化再锁）

1. 专家通关 ≥ 16/20（环境可解）
2. 冠军通关 ≥ 16/20
3. 冠军落后专家不超过 2/20
4. 冠军转向 `mean|Δneg| < 0.80`（无狂抖）

## 实测（不得改写成论文级 100% 迷宫逃逸的旧口径）

```
GATE6_MAZE expert=20/20 champ=20/20 mean_|dneg|=0.305835
PASS maze Gate 6 shadow vs geodesic expert
```

obs[3] P 控制专家在同协议下 **0/20**，已弃用为影子基线。冠军 20 局全部比专家步数更长，只证明「能到」而非「比势场更快」。
